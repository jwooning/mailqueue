[Unit]
Description=Headless chrome autmation tool
After=network-online.target

[Service]
Type=simple
Restart=always
RestartSec=60
WorkingDirectory=<WORKING_DIR>
ExecStart=<PIPENV_PATH> run ./manager

[Install]
WantedBy=multi-user.target
